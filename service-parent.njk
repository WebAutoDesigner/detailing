---
layout: base.njk
pagination:
  data: parent_services_multi
  size: 1
  alias: svc
  addAllPagesToCollections: true
permalink: /{{ svc.slug }}/
bodyClass: page-subservice
---

<!-- HERO -->
<section class="page-hero page-hero--full" style="--hero-bg: url('{{ svc.img }}'); --hero-bg-mobile: url('{{ svc.imgMobile }}')">
  <div class="page-hero__overlay"></div>
  <div class="page-hero__content">
    <div class="breadcrumb">
      <a href="/">Главная</a>
      <span>/</span>
      <span>{{ svc.title }}</span>
    </div>
    <h1>{{ svc.title }}</h1>
    <p>{{ svc.desc }}</p>
  </div>
</section>

<!-- SUBSERVICES -->
{% if svc.subservices | length %}
<section class="subservices">
  <div class="subservices__inner">
    <h2 class="section-title">// Выберите услугу</h2>
    <div class="subservices__grid">
      {% for sub in svc.subservices %}
      <a href="/{{ sub.slug }}" class="subservice-card">
        <div class="subservice-card__body">
          <h3>{{ sub.name }}</h3>
          <p>{{ sub.desc }}</p>
        </div>
        <span class="subservice-card__arrow">→</span>
      </a>
      {% endfor %}
    </div>
  </div>
</section>
{% endif %}

<!-- DESCRIPTION -->
{% if svc.descBlocks | length %}
<section class="service-desc">
  <div class="service-desc__inner">
    {% for block in svc.descBlocks %}
    <div class="service-desc__block">
      <h2>{{ block.h2 }}</h2>
      <p>{{ block.text }}</p>
    </div>
    {% endfor %}
  </div>
</section>
{% endif %}

<!-- CTA FORM -->
<section class="cta-form contacts-cta" id="contact-form">
  <div class="cta-form__bg" style="--cta-bg-desk: url('{{ pagedata.worksHeroBg }}'); --cta-bg-mob: url('{{ site.ctaBg }}')"></div>
  <div class="contacts-cta__inner">
    <h2>Рассчитать стоимость</h2>
    <p>Оставьте свой номер телефона и наш менеджер свяжется с вами в ближайшее время</p>
    <form class="cta-form__form" id="ctaForm">
      <input type="hidden" name="service" value="{{ svc.title }}">
      <input type="text" name="name" placeholder="Ваше имя" required>
      <input type="tel" name="phone" placeholder="Номер телефона" required>
      <label class="cta-form__checkbox">
        <input type="checkbox" name="agree" required>
        <span>Нажимая кнопку, вы даёте согласие на обработку персональных данных</span>
      </label>
      <button type="submit" class="btn btn--outline">Получить консультацию</button>
    </form>
  </div>
</section>

<!-- REVIEWS -->
<section class="reviews reviews--cards">
  <div class="reviews__header">
    <h2 class="section-title">// Отзывы</h2>
  </div>
  <div class="reviews__slider-outer">
    <button class="reviews__arrow" id="reviewsPrev" aria-label="Предыдущие">
      <svg width="10" height="18" viewBox="0 0 10 18" fill="none"><path d="M9 1L1 9l8 8" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </button>
    <div class="reviews__overflow">
      <div class="reviews__track" id="reviewsTrack">
        {% for review in pagedata.reviews %}
        <div class="review-card">
          <div class="review-card__logo">
            <span class="review-card__logo-y">Я</span><span class="review-card__logo-t">ндекс</span>
          </div>
          <div class="review-card__top">
            <div class="review-card__avatar">{{ review.avatar }}</div>
            <div>
              <div class="review-card__name">{{ review.name }}</div>
              <div class="review-card__stars">{% for i in range(0, review.rating) %}★{% endfor %}</div>
            </div>
          </div>
          <p class="review-card__text">{{ review.text }}</p>
        </div>
        {% endfor %}
      </div>
    </div>
    <button class="reviews__arrow" id="reviewsNext" aria-label="Следующие">
      <svg width="10" height="18" viewBox="0 0 10 18" fill="none"><path d="M1 1l8 8-8 8" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </button>
  </div>
  <div class="reviews__dots" id="reviewsDots"></div>
  {% if site.reviewsWidgetUrl %}
  <div class="reviews__all-wrap">
    <a href="{{ site.reviewsWidgetUrl | replace('maps-reviews-widget/', 'maps/org/') | replace('?comments', '/reviews/') }}" target="_blank" rel="noopener" class="btn btn--outline">Читать все отзывы</a>
  </div>
  {% endif %}
</section>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "Главная", "item": "{{ site.siteUrl }}/" },
    { "@type": "ListItem", "position": 2, "name": "{{ svc.title }}", "item": "{{ site.siteUrl }}/{{ svc.slug }}/" }
  ]
}
</script>

<!-- FAQ -->
{% if svc.faq and svc.faq | length %}
<section class="faq-section">
  <div class="faq-section__inner">
    <h2 class="section-title">// Ответы на вопросы</h2>
    <div class="faq-list">
      {% for item in svc.faq %}
      <div class="faq-item">
        <button class="faq-item__btn" aria-expanded="false">
          <span class="faq-item__num">{% if loop.index < 10 %}0{{ loop.index }}{% else %}{{ loop.index }}{% endif %}</span>
          <span class="faq-item__q">{{ item.q }}</span>
          <span class="faq-item__icon">+</span>
        </button>
        <div class="faq-item__answer">
          <p>{{ item.a }}</p>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>
{% endif %}
